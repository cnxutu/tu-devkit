name: tu-devkit CI

on:
  push:
  pull_request:

jobs:
  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Bash tests
        shell: bash
        run: |
          bash dev-env-init/tests/run.sh
          bash vps-init/tests/run.sh

      - name: Run syntax checks
        shell: bash
        run: bash -n dev-env-init/install.sh dev-env-init/bin/tu dev-env-init/bin/tu-wrapper dev-env-init/lib/*.sh dev-env-init/scripts/*.sh dev-env-init/tests/*.sh vps-init/install.sh vps-init/doctor.sh vps-init/lib/*.sh vps-init/scripts/*.sh vps-init/tests/*.sh

      - name: Install ShellCheck
        shell: bash
        run: |
          if command -v shellcheck >/dev/null 2>&1; then
            exit 0
          elif [[ "$RUNNER_OS" == Linux ]]; then
            sudo apt-get update
            sudo apt-get install -y shellcheck
          else
            brew install shellcheck
          fi

      - name: Run ShellCheck
        shell: bash
        run: shellcheck --severity=warning dev-env-init/install.sh dev-env-init/bin/tu dev-env-init/bin/tu-wrapper dev-env-init/lib/*.sh dev-env-init/scripts/*.sh dev-env-init/tests/*.sh vps-init/install.sh vps-init/doctor.sh vps-init/lib/*.sh vps-init/scripts/*.sh vps-init/tests/*.sh

      - name: Validate AI guidance
        shell: bash
        run: bash ai-guidance/tests/test-guidance-validation.sh

      - name: Validate shared Codex plugin
        shell: bash
        run: bash ai-guidance/plugins/ai-guidance-workflows/tests/test-plugin.sh
